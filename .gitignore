# Dipendenze
node_modules/
.pnpm-store/
vendor/

# Build
dist/
# `nuxi generate` crea anche un symlink `dist` -> `.output/public` con path
# assoluto: il pattern `dist/` non intercetta i symlink, quindi serve anche
# questa riga.
dist
.output/
.nuxt/
.nitro/
bin/
*.exe

# Ambiente e segreti
.env
.env.*
!.env.example
*.pem
*.key

# Test e coverage
coverage/
*.out
test-results/
playwright-report/

# Log delle corse di `make ci`, uno per corsa (scripts/ci-parallel.sh).
# Sono prove da leggere dopo un fallimento, non artefatti da versionare: la
# rotazione dello script tiene la directory piccola da sola.
.ci-logs/

# Locale / editor
.DS_Store
.idea/
*.swp
.tmp/

# Archivi legacy (mai versionare)
*.bundle

# Worktree delle issue in lavorazione (AGENTS.md §3)
.worktrees/
