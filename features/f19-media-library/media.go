// Package medialibrary owns reusable media ingestion, metadata search,
// composer references, quota enforcement, and conservative object lifecycle.
package medialibrary

const FeatureID = "f19-media-library"

// QuotaResource is the server-only F10 resource consumed by this slice.
const QuotaResource = "media_storage_bytes"
